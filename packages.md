Required packages:
```sh
pip install -U langchain langchain-community langchainhub langchain_huggingface
pip install -U langchain-openai langchain-groq
pip install -U pypdf docx2txt 
pip install -U pinecone-client langchain-pinecone

```

